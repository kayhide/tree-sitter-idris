(signature
  name: (variable)) @local.definition

(function
  (lhs
    (funvar
      name: (variable)))) @local.definition

(pat_name
  (variable)) @local.definition

(exp_name
  (variable)) @local.reference

